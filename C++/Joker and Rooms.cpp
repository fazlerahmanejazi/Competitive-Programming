#include <bits/stdc++.h>
using namespace std ;

int main()
{
  int T ; long temp, N, M ;
  cin>>
  while(T--)
  { set<long>::iterator it ;
    map<long,long> a ;
    set<long> b ;
    cin>> N >> M ;
    while(M--)
    {
      cin>> temp ;
      b.insert(temp) ;
    }
    it=b.end() ;
    it-- ;
    if(*it==b.size())

  }
